{
  "name": "devinsight-guardian-lambda",
  "version": "1.0.0",
  "description": "DevInsight Guardian — Autonomous AI Engineering Manager Lambda",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136",
    "@aws-sdk/client-ses": "^3.600.0",
    "@aws-sdk/client-cloudwatch": "^3.600.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
